Candidate Portal module

Entry: `lib/candidate_portal/main_candidate.dart`

Run (from project root):

```
flutter run -t lib/candidate_portal/main_candidate.dart
```

Notes:
- Uses Provider for state management.
- Integrates with existing backend REST endpoints under `http://localhost:3000` by default (change in `AuthProvider`).
